# Col 列布局组件测试用例

## 组件信息

- **组件名称**: nv-col
- **组件路径**: `src/components/col`
- **测试优先级**: P1 (中优先级)
- **组件描述**: 栅格系统的列组件

## 1. 功能测试

### 1.1 基础渲染

- **测试ID**: `col-basic-render`
- **测试步骤**:
  1. 在 row 中渲染 col 组件
  2. 验证 col 正常显示
- **预期结果**: col 正常渲染

## 2. 属性测试

### 2.1 span 属性 - 列宽

- **测试ID**: `col-span-{value}`
- **测试值**: 1-24
- **测试步骤**:
  1. 设置不同的 span 值
  2. 验证列宽度
- **预期结果**: 列宽度按 24 栅格系统正确显示

### 2.2 offset 属性 - 偏移

- **测试ID**: `col-offset-{value}`
- **测试值**: 1-24
- **测试步骤**:
  1. 设置 offset 属性
  2. 验证列左侧偏移
- **预期结果**: 列正确偏移

### 2.3 push 属性 - 右移

- **测试ID**: `col-push-{value}`
- **测试步骤**:
  1. 设置 push 属性
  2. 验证列向右移动
- **预期结果**: 列正确右移

### 2.4 pull 属性 - 左移

- **测试ID**: `col-pull-{value}`
- **测试步骤**:
  1. 设置 pull 属性
  2. 验证列向左移动
- **预期结果**: 列正确左移

## 测试覆盖率目标

- **功能覆盖率**: 100%
- **属性覆盖率**: 100%
